<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= render 'layouts/rails_default' %>
    <%= render 'layouts/shim' %>
  </head>

  <body>
    <%= render 'layouts/header' %>
    <div class="container body">
      <!-- devise >>> -->
      <% if notice.present? %>
          <p id="notice" class="class: alert alert-info"><%= notice %></p>
      <% end %>
      <% if alert.present? %>
          <p id="alert" class="class: alert alert-danger"><%= alert %></p>
      <% end %>

      <!--<% if user_signed_in? %>
        <p><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></p>
      <% end %>-->
      <!-- <<< devise -->
      <%= yield %>
      <%= render 'layouts/footer' %>
    </div>
  </body>
</html>
